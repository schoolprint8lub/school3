<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }} | Школа №3 м. Лубни</title>
    <meta name="description" content="Офіційний сайт Школи №3 міста Лубни">
    
    <!-- Підключення стилів -->
    <link rel="stylesheet" href="/css/style.css">
    
    <!-- Netlify Identity -->
    <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>
</head>
<body>
    <header>
        <div class="container header-inner">
            <a href="/" class="logo">🏫 <span>Школа №3</span></a>
            <nav>
                {% macro renderNavListItem(entry) -%}
                <li>
                  <a href="{{ entry.url }}">{{ entry.title }}</a>
                  {%- if entry.children.length -%}
                  <ul class="dropdown">
                    {%- for child in entry.children %}{{ renderNavListItem(child) }}{% endfor -%}
                  </ul>
                  {%- endif -%}
                </li>
                {%- endmacro %}

                <ul>
                    <li><a href="/">Головна</a></li>
                    {%- for entry in collections.all | eleventyNavigation %}
                        {{ renderNavListItem(entry) }}
                    {%- endfor %}
                    <li><a href="/admin/" class="btn" style="padding: 8px 16px; border-radius: 6px; font-size: 0.9em; margin-left: 10px;">Вхід</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <main>
        <div class="container">
            <article class="content-wrapper">
                {{ content | safe }}
            </article>
        </div>
    </main>

    <footer>
        <div class="container">
            <div class="footer-grid">
                <div class="footer-col">
                    <h3>Школа №3 м. Лубни</h3>
                    <p>Навчальний заклад нового типу, де кожен учень - особистість.</p>
                </div>
                <div class="footer-col">
                    <h3>Контакти</h3>
                    <p>📍 вул. Монастирська, 35<br>📞 (05361) 7-22-33<br>✉️ school3@example.com</p>
                </div>
                <div class="footer-col">
                    <h3>Навігація</h3>
                     <p><a href="/admin/" style="color: inherit; text-decoration: underline;">Адміністрування</a></p>
                </div>
            </div>
            <div class="footer-bottom">
                <p>&copy; 2024 Лубенська спеціалізована школа №3. Всі права захищено.</p>
            </div>
        </div>
    </footer>

    <script>
        if (window.netlifyIdentity) {
            window.netlifyIdentity.on("init", user => {
                if (!user) {
                    window.netlifyIdentity.on("login", () => {
                        document.location.href = "/admin/";
                    });
                }
            });
        }
    </script>
</body>
</html>
